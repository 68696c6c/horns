---
name: 'Fieldset'
menu: Forms
---

import { Playground, Props } from 'docz'

import Fieldset from './fieldset'
import Group from './group'
import { Input, Textarea } from './inputs'
import { Checkbox, Radio } from './toggles'
import { Select } from './selects'

# Fieldset

<Playground>
  <Fieldset legend="Fieldset">
    <Input label="Input inside of a Fieldset" />
    <Textarea label="Textarea inside of a Fieldset" />
    <Checkbox label="Checkbox inside of a Fieldset" />
    <Checkbox label="Checkbox inside of a Fieldset" />
    <Group>
      <Radio label="Grouped Radio inside of a Group" />
      <Radio label="Grouped Radio inside of a Group" />
      <Radio label="Grouped Radio inside of a Group" />
    </Group>
    <Select label="Select inside of a Fieldset">
      <option>Option 1</option>
    </Select>
  </Fieldset>
  <Fieldset legend="Fieldset with a single field">
    <Input label="Single Input inside of a Fieldset" />
  </Fieldset>
</Playground>

## Props

<Props of={Fieldset} />
