---
name: 'Group'
menu: Forms
---

import { Playground, Props } from 'docz'

import Group from './group'
import { Input, Textarea } from './inputs'
import { Checkbox, Radio } from './toggles'
import { Select } from './selects'
import Section from '../layout/section'

# Group

Groups can be used to collect form inputs into a single container for styling purposes.  Groups are especially useful
for collecting a set of Checkboxes or Radios under a single label.  Groups automatically wrap their children in their 
own containers, but do not provide any responsive behavior.

<Playground>
    <Input label="Input outside of a Group" />
    <Textarea label="Textarea outside of a Group" />
    <Checkbox label="Checkbox outside of a Group" />
    <Radio label="Radio outside of a Group" />
    <Select label="Select outside of a Group">
      <option>Option 1</option>
    </Select>
  <Group>
    <Input label="Input inside of a Group" />
    <Textarea label="Textarea inside of a Group" />
    <Checkbox label="Checkbox inside of a Group" />
    <Radio label="Radio inside of a Group" />
    <Select label="Select inside of a Group">
      <option>Option 1</option>
    </Select>
  </Group>
  <Group heading="Optional Group header">
    <Radio label="Radio inside of a Group" />
    <Radio label="Radio inside of a Group" />
    <Radio label="Radio inside of a Group" />
    <Radio label="Radio inside of a Group" />
  </Group>
</Playground>

## Props

<Props of={Group} />
