---
name: 'Pagination'
menu: Navs
---

import { Playground, Props } from 'docz'

import Pagination from './pagination'

# Pagination

<Playground>
  <Pagination pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
</Playground>

## Props

<Props of={Pagination} />

## Colorways

<Playground>
  <Pagination pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="prominent" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="primary" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="secondary" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="tertiary" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="light" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="neutral" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="dark" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="success" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="info" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="warning" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
  <Pagination colorway="danger" pages={10} page={1} onChange={
    (page, callback) => {
      console.log(page)
      callback(10)
    }}
  />
  <br />
</Playground>
